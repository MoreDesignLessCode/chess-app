// Chesser runtime config (loaded before app.js).
// Set CHESSER_SERVER to your online server's host to turn ON live online play + World chat.
// It should be JUST the host, no "https://" and no trailing slash, e.g.:
//     window.CHESSER_SERVER = "chesser-server.fly.dev";
// Leave it "" to use the same site you're on (that's how local `npm run online` works).
window.CHESSER_SERVER = "";
